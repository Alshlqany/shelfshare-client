import LoginForm from "../components/Auth/LoginForm";
import Auth from "../components/Auth";

const Login = () => {
  return (
    <Auth>
      <LoginForm />
    </Auth>
  );
};

export default Login;
